import { ProjectImageService_generated } from './bll.generated';

/**
 * Business logic (service) classes for model in typescript (nest.js) for ProjectImage
 * NOTE: EDIT AT WILL, this is intially generated and then left alone. Your changes will not be affected by subsequent code generation runs
 * one image for one project or project entity. if project entity is filled in it for that entity otherwise its for the project
 */

/**
 * ProjectImage
 * one image for one project or project entity. if project entity is filled in it for that entity otherwise its for the project
 */
export class ProjectImageService extends ProjectImageService_generated {
	/**
	 * place your custom code here
	 */
}


