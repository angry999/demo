import { BackgroundProcessService_generated } from './bll.generated';

/**
 * Business logic (service) classes for model in typescript (nest.js) for BackgroundProcess
 * NOTE: EDIT AT WILL, this is intially generated and then left alone. Your changes will not be affected by subsequent code generation runs
 * one job that needs to be run in the background
 */

/**
 * BackgroundProcess
 * one job that needs to be run in the background
 */
export class BackgroundProcessService extends BackgroundProcessService_generated {
	/**
	 * place your custom code here
	 */
}


