import { UserInvitationService_generated } from './bll.generated';

/**
 * Business logic (service) classes for model in typescript (nest.js) for UserInvitation
 * NOTE: EDIT AT WILL, this is intially generated and then left alone. Your changes will not be affected by subsequent code generation runs
 * an invitation from one user to another user to join the system
 */

/**
 * UserInvitation
 * an invitation from one user to another user to join the system
 */
export class UserInvitationService extends UserInvitationService_generated {
	/**
	 * place your custom code here
	 */
}


