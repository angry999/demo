import { ProjectBalanceAdjustmentService_generated } from './bll.generated';

/**
 * Business logic (service) classes for model in typescript (nest.js) for ProjectBalanceAdjustment
 * NOTE: EDIT AT WILL, this is intially generated and then left alone. Your changes will not be affected by subsequent code generation runs
 * one change in the balance of a project for one financial period
 */

/**
 * ProjectBalanceAdjustment
 * one change in the balance of a project for one financial period
 */
export class ProjectBalanceAdjustmentService extends ProjectBalanceAdjustmentService_generated {
	/**
	 * place your custom code here
	 */
}


