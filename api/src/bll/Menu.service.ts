import { MenuService_generated } from './bll.generated';

/**
 * Business logic (service) classes for model in typescript (nest.js) for Menu
 * NOTE: EDIT AT WILL, this is intially generated and then left alone. Your changes will not be affected by subsequent code generation runs
 * one item in a menu including menus themselves
 */

/**
 * Menu
 * one item in a menu including menus themselves
 */
export class MenuService extends MenuService_generated {
	/**
	 * place your custom code here
	 */
}


